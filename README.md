# Web Studio

Build a website with the agent and preview it live in the app.

A Möbius mini-app. Install it from the in-app App Store, or:

```
POST /api/apps/install { "manifest_url": "https://raw.githubusercontent.com/mobius-os/app-webstudio/main/mobius.json" }
```
