{
  "name": "app-webstudio",
  "version": "1.3.4",
  "private": true,
  "type": "module",
  "description": "Web Studio — Mobius catalog app. Dev tooling only; the app itself ships via mobius.json source_files.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs && bash -n project-builder.sh",
    "smoke": "node tests/smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
